import React, { useState } from 'react';

export const Legend = (props) => {
    return (
        <div className='legend-el'><span></span><button className='nonmetal example'>Niemetale</button></div>
    );
}


export default Legend;